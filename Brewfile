# For Qt (defaults to latest, which is Qt6)
brew "qt"

# For CGAL
brew "cgal"

# For Ceres (area smoothing in CGAL)
brew "ceres-solver"

# Eigen (Ceres/CGAL dependency)
brew "eigen"

# For Git LFS (large files)
brew "git-lfs"
